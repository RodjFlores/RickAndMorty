export enum SortDirection{
    ASC = 'asc',
    DESC = 'desc'
  }
  
  export enum SortTerm{
    NAME = 'name',
    STATUS = 'status',
    SPECIES = 'species',
    EPISODES = 'episodes'
  }
  
  export enum CharacterStatus{
    UNKNOWN = 'unknown',
    DEAD = 'dead',
    ALIVE = 'alive',
  }